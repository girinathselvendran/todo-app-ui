import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react_ioni_cpropject',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
